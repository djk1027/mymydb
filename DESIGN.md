# 간단한 관계형 데이터페이스 서버/클라이언트

## 사용 언어 / 컴파일 환경
- 언어: C (C11 표준)
- 컴파일러: gcc (clang도 호환)
- 빌드: Makefile
- 대상 환경: 범용 리눅스 환경(x86-64) 

## 진행 방향
- parser / optimizer / executor를 나눈다.

## v0
- 파싱: SELECT/INSERT/CREATE TABLE, WHERE까지.
- 저장: 인메모리 테이블 (행 배열)
- executor: full scan + WHERE 필터
- 옵티마이저: 없음 (무조건 full scan)

## v1 (완료)
- 테스트 프로그램을 만들고 케이스를 여러개 작성할 것 (overflow, 대용량 select, 대용량 insert 등)
  → `tests/` (`make test`). overflow / 100k insert / 대용량·필터 select / 에러 경로 등
- 벤치마크 테스트 프로그램을 실행시킬 수 있는 벤치마크 프로그램을 만들것
  → `bench/` (`make bench [N]`). insert 처리량, full scan, WHERE 선택도별 처리량 측정
- 용도별로 디렉터리를 분리할 것 (parse, optimize, execute 같이)
  → `src/{parse,optimize,execute,storage,cli}/`
- 메모리 누수 등 프로그램 안정성 테스트를 같이 포함
  → `make memcheck` (valgrind, leak=실패). 파서를 arena 기반으로 바꿔 에러 경로 누수 제거

## v1.1 (완료)
- 실행마다 결과를 git 커밋 멘트로 사용할 수 있도록 한글/영어로 정리할 것 (version 디렉터리를 만들고 거기에 내용 저장)
- git을 직접 commit, merge 하지 않을 것
- DESIGN.md 파일은 이후 프롬프트 추정을 위해 수정하지 않을것


## v1.2 (완료)
- I/O에 page 아키텍처를 이용함 1page 사이즈는 4K로 한다.
- COUNT, SUM, AVG, MIN, MAX 윈도우 함수를 추가한다.
- ORDER BY 기능을 추가한다.

## v1.3 (완료)
- cli 버퍼가 비어있는 상태에서 enter 키 입력시 mymydb> 형태의 프롬프트를 출력한다.

## v2.0 (완료)
### 기능
- DELETE 기능을 추가한다. 

### 설계
- DB의 모든 구조를 오브젝트로 관리한다 오브젝트는 object id, type으로 이루어져 있다. 현 상태에서 오브젝트는 db, table 두 가지가 있다.  
- 오브젝트 종류별로 구조체를 가진다. 현재 테이블 구조체는 있으므로 database 구조체를 생성하고 관리한다. database는 인스턴스 내에서 논리적으로 나눠진다.
- 오브젝트 종류별로 파일을 나눠 관리한다.
- 페이지 별로 고유한 page id들 가진다 page id는 db id, 오브젝트 id, 시퀀스로 이루어지며 중복이 존재해선 안된다.
- 스토리지 저장 기능을 추가한다. 저장은 페이지 단위로 이뤄진다.
- 할당은 1page 사이즈를 기본 8K로 수정하고 Block으로 명명한다.
- 블록 할당은 익스텐트 단위로 이뤄진다. 익스텐트는 초기 8 Block으로 시작해 사이즈를 초과하면 더블링 할당한다.
- 시퀀셜 스캔 시 커널 LBA의 연속성이 오브젝트 내에서 잘 이뤄 질 수 있도록한다.


## v2.1 (완료)
### 기능
- dot 명령어를 모두 일반 문자열 파싱으로 바꾼다 (. 문자 없이 동작하도록)
- db 파라미터를 생성한다. 우선 블록 사이즈, 데이터 파일 위치 설정만 추가한다.
- db 파라미터 또한 테이블 형태로 저장하고 db 생성할 때 기본값으로 가진다.
- db 파라미터는 기본적으로 db별로 나눠져 있으며 인스턴스 공통으로 적용돼야 할 사항은 글로벌 파라미터로 만든다. 
- 설정 파일을 만들어 파라미터를 관리한다. 여기에 데이터 파일 위치, 블록 사이즈 등의 기본 설정을 할 수 있으며 파라미터의 내용을 공유한다 기본설정을 주석으로 기입해 추후 사용자가 편하게 변경할 수 있도록한다. 파일명은 mymy.conf


## v2.2
### 기능
- exit help 등 기존 명령어는 세미콜론 없이 실행하도록 설정한다.
- 인 메모리 기능을 삭제한다.

### 설계
- 빌드 경로를 지정한다. $MYMY/mymydb를 기본 경로로 한다. 기본 경로 아래 bin, data 디렉터리를 가진다.
- 파라미터 파일의 데이터 경로를 무조건 활용하도록 한다. 기본 데이터 경로는 기본 경로 아래  data 디렉터리로 정한다.
- 파일을 여러개로 나눈다 db별로 물리적으로 분리한다 